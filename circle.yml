machine:
  environment:
    PATH_TO_BUILD: ./public
    SURGE_DOMAIN_DEV: ecomchicago-dev.surge.sh
    SURGE_DOMAIN_PROD: ecomchicago.surge.sh
  node:
    version: 6.9.1

test:
  override:
    - echo "Just ship it! 📦"
  post:
    - gatsby build --prefix-links

deployment:
  development:
    branch: develop
    commands:
      - ./node_modules/.bin/surge --project $PATH_TO_BUILD --domain $SURGE_DOMAIN_DEV
  production:
    branch: master
    commands:
      - ./node_modules/.bin/surge --project $PATH_TO_BUILD --domain $SURGE_DOMAIN_PROD

